Hibernate CRUD Project

A simple Java project demonstrating CRUD (Create, Read, Update, Delete) operations using Hibernate ORM (latest version).

Features

Uses Hibernate 6+

Connects to MySQL database

Performs basic CRUD operations:

Create new records

Read records from the database

Update existing records

Delete records
